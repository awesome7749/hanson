services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: hanson
      POSTGRES_PASSWORD: hanson_dev
      POSTGRES_DB: hanson
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
